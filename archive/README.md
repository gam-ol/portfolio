# Legacy site backup

`legacy-site/` is an exact snapshot of the original static website from `origin/main` before the Astro migration.

The original entry point is [`legacy-site/index.html`](legacy-site/index.html). The folder is kept for reference and is not included in the Netlify production build.
